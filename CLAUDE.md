# Claude Code開発ガイドライン

## プロジェクト概要

このリポジトリは、Nix/NixOSとHome Managerを使用したポータブルなdotfiles設定を提供します。異なるユーザーやシステム間で一貫した開発環境を構築することを目的としています。

## 開発方針

### 1. 移植性の重視
- ハードコードされたパスやユーザー名を避ける
- 環境変数や関数パラメータを活用した動的な設定
- 異なるOS・アーキテクチャでの動作を考慮

### 2. コードの保守性
- 設定の変更や追加が容易な構造
- 明確な責任分離（プログラムごと、機能ごと）
- 十分なコメントとドキュメント

### 3. Nixエコシステムのベストプラクティス
- Pure evaluation mode vs Impure modeの適切な使い分け
- Flakesを活用したモダンなNix設定
- 依存関係の明示的な管理

## ファイル構成ルール

### ディレクトリ構造
```
├── プログラム名/          # 各プログラムの設定ファイル
├── home-manager/         # Home Manager関連の設定
└── scripts/             # セットアップ・管理スクリプト
```

### 命名規則
- 設定ファイル：プログラム名に対応（例：`.zshrc`, `.gitconfig`）
- スクリプト：動詞で始まる（例：`setup-environment.sh`）
- ドキュメント：大文字で始まる（例：`README.md`, `USAGE.md`）

## 変更・追加時の注意点

### 新しい設定ファイルを追加する場合
1. 既存の類似設定を参考にする
2. パス参照は相対パスまたはパラメータ化
3. コメントで設定の意図を明記
4. 対応するドキュメントを更新

### 既存設定を変更する場合
1. 変更理由を明確にする
2. 下位互換性への影響を検討
3. テスト（可能であればDocker等での動作確認）
4. 関連ドキュメントの更新

## 品質保証

### テスト方針
- 可能な限りDocker環境での動作確認
- 異なるユーザー名・ホームディレクトリでのテスト
- 環境変数ベース設定の動作確認

### ドキュメント要件
- READMEファイルによる使用方法の説明
- 設定ファイル内のコメントによる説明
- 変更点の適切なgitコミットメッセージ

## トラブルシューティング

### よくある問題
1. **パスの参照エラー**: 絶対パスの使用を避け、相対パスやパラメータ化を検討
2. **環境変数の取得失敗**: `--impure`フラグの使用または明示的な設定を検討
3. **権限エラー**: シンボリックリンク作成時の権限設定を確認

### デバッグ手法
- `nix-instantiate --eval`でNix式の評価結果を確認
- `home-manager --dry-run`で変更内容の事前確認
- Docker環境でのクリーンな状態でのテスト

## 参考資料

- [Home Manager Manual](https://nix-community.github.io/home-manager/)
- [Nix Manual](https://nixos.org/manual/nix/stable/)
- [Nixpkgs Manual](https://nixos.org/manual/nixpkgs/stable/)