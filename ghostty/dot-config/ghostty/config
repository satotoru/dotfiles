# =============================================================================
# Ghostty Configuration
# =============================================================================
# cmux互換: フォント・テーマ・ターミナル設定はcmuxでもそのまま使える
# cmux非互換: keybind設定はGhostty単体でのみ有効（cmuxは独自キーバインド）

# -----------------------------------------------------------------------------
# Font
# -----------------------------------------------------------------------------
# HackGen Console NF: 英数=Hack + 日本語=源柔ゴシック の合成フォント (1:2幅)
# Nerd Font グリフ込みなのでアイコン表示にも対応
font-family = HackGen Console NF
font-size = 12
font-thicken = true

# -----------------------------------------------------------------------------
# Theme / Colors
# -----------------------------------------------------------------------------
# theme = catppuccin-mocha
background-opacity = 0.95

# -----------------------------------------------------------------------------
# Window
# -----------------------------------------------------------------------------
window-padding-x = 4
window-padding-y = 4
window-padding-balance = true
window-save-state = always
macos-titlebar-style = tabs
macos-option-as-alt = left

# -----------------------------------------------------------------------------
# Cursor
# -----------------------------------------------------------------------------
cursor-style = block
cursor-style-blink = false

# -----------------------------------------------------------------------------
# Notifications
# -----------------------------------------------------------------------------
desktop-notifications = true
# 長時間コマンド終了時に通知（フォーカスが外れている場合のみ）
notify-on-command-finish = unfocused
notify-on-command-finish-after = 5s

# Shell / Terminal
# -----------------------------------------------------------------------------
mouse-hide-while-typing = true
clipboard-read = allow
clipboard-write = allow
clipboard-trim-trailing-spaces = true
copy-on-select = clipboard

# tmux併用時はGhostty側のsplit設定は不要（tmuxが管理する）
